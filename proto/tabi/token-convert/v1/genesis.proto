syntax = "proto3";
package tabi.token_convert.v1;

import "gogoproto/gogo.proto";

import "tabi/token-convert/v1/convert.proto";

option go_package = "github.com/tabilabs/tabi/x/token-convert/types";

// GenesisState defines the token module's genesis state
message GenesisState {
  repeated LockRuleInfo lock_rule_infos = 1 [(gogoproto.nullable) = false];
  repeated UserUnlockInfo user_unlock_infos = 2 [(gogoproto.nullable) = false];
}



